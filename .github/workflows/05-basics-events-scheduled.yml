name: 05 Basics Events Scheduled

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World"
